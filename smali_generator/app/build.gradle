plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'com.smali_generator'
    compileSdk 36

    defaultConfig {
        applicationId "com.smali_generator"
        minSdk 33
        //noinspection ExpiredTargetSdkVersion
        targetSdk 36
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    // This module reflects into the host app by design, and its class literals are
    // unsubstituted templates until patch time. PrivateApiDetector resolves those as
    // Java types and crashes on anything unparseable -- a placeholder, or an obfuscated
    // name like X.7WU -- which fails the build from inside lint itself.
    lint {
        disable 'PrivateApi', 'BlockedPrivateApi', 'DiscouragedPrivateApi', 'SoonBlockedPrivateApi'
    }
}

dependencies {
    implementation libs.arthooks
    implementation libs.androidx.annotation.jvm
}

tasks.whenTaskAdded { task ->
    if (task.name == 'assembleRelease') {
        task.doLast {
            def apkDir = "$buildDir/outputs/apk/release"
            def apkFile = fileTree(dir: apkDir, includes: ["*.apk"]).singleFile
            def destinationDir = "./"

            mkdir(destinationDir)
            copy {
                from apkFile
                into "$destinationDir"
            }
            def output_apk = file("$destinationDir/${apkFile.name}")
            output_apk.renameTo("${destinationDir}/smali_generator.apk")
            println "APK moved to $destinationDir"
        }
    }
}